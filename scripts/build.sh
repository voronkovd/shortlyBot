#!/bin/bash

# Скрипт для сборки Docker образа бота

set -e

echo "🔨 Сборка Docker образа ShortlyBot..."

# Проверяем наличие .env файла
if [ ! -f .env ]; then
    echo "❌ Файл .env не найден!"
    echo "📝 Скопируйте env.example в .env и заполните TELEGRAM_BOT_TOKEN"
    exit 1
fi

# Собираем образ
docker-compose build bot

echo "✅ Docker образ успешно собран!"
echo "🚀 Для запуска используйте: docker-compose up -d"
