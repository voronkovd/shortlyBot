# ShortlyBot - Telegram Video Downloader

Telegram бот для скачивания коротких видео с популярных социальных платформ.

## 🎬 Поддерживаемые платформы

- **Instagram** - посты, reels, IGTV
- **TikTok** - короткие видео
- **YouTube Shorts** - короткие видео
- **Likee** - короткие видео
- **Facebook Reels** - короткие видео
- **RuTube** - видео и shorts

## ✨ Возможности

- 🔄 Автоматическое определение платформы по URL
- ⚡ Быстрое скачивание с оптимизированными настройками
- 🗑️ Удаление исходного сообщения после успешной отправки
- 🌍 Поддержка русского и английского языков
- 📊 Сбор статистики через RabbitMQ
- 🧪 Полное тестирование (84% покрытие)

## 🚀 Быстрый старт

### 1. Клонирование и настройка
```bash
git clone <repository-url>
cd shortlyBot
cp env.example .env
# Отредактируйте .env и добавьте ваш TELEGRAM_BOT_TOKEN
```

### 2. Запуск с Docker
```bash
# Сборка и запуск
docker-compose up -d --build

# Проверка статуса
docker-compose ps
```

### 3. Проверка работы
- **RabbitMQ Management UI**: http://localhost:15672 (admin/password123)
- **Логи бота**: `docker-compose logs -f bot`
- **Остановка**: `docker-compose down`

## 🛠️ Локальная разработка

### 1. Настройка окружения
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
pip install -r requirements.txt
```

### 2. Запуск RabbitMQ
```bash
docker-compose -f docker-compose.dev.yml up -d
```

### 3. Запуск бота
```bash
python main.py
```

## 📊 Мониторинг

### Основные команды
```bash
# Статус сервисов
docker-compose ps

# Логи бота
docker-compose logs -f bot

# Перезапуск бота
docker-compose restart bot

# Очистка данных (осторожно!)
docker-compose down -v
```

### RabbitMQ Management UI
- **URL**: http://localhost:15672
- **Логин**: admin
- **Пароль**: password123

## 📁 Структура проекта

```
shortlyBot/
├── main.py                 # Основной файл бота
├── handlers/               # Обработчики запросов
│   └── downloader.py      # Обработчик загрузки видео
├── providers/             # Провайдеры для различных платформ
│   ├── base.py           # Базовый класс провайдера
│   ├── instagram.py      # Instagram провайдер
│   ├── tiktok.py         # TikTok провайдер
│   ├── youtube.py        # YouTube провайдер
│   ├── likee.py          # Likee провайдер
│   ├── facebook.py       # Facebook провайдер
│   └── rutube.py         # RuTube провайдер
├── commands/              # Команды бота
│   ├── start.py          # Команда /start
│   └── help.py           # Команда /help
├── analytics/             # Система аналитики
│   ├── stats_collector.py # Сбор статистики
│   └── rabbitmq_client.py # RabbitMQ клиент
├── localization/          # Система локализации
│   ├── translations.py   # Переводы
│   └── utils.py          # Утилиты локализации
├── tests/                 # Тесты (84% покрытие)
├── scripts/               # Скрипты автоматизации
├── docker-compose.yml     # Docker Compose конфигурация
└── requirements.txt       # Python зависимости
```

## 🎯 Использование

1. Запустите бота командой `/start`
2. Отправьте ссылку на видео с поддерживаемой платформы
3. Дождитесь обработки и получения видео

## 📋 Команды

- `/start` - Начать работу с ботом
- `/help` - Показать справку по использованию

## ⚠️ Ограничения

- Работает только с публичными аккаунтами
- Максимальный размер видео: 50MB
- Некоторые видео могут быть недоступны
- YouTube Shorts: только видео до 60 секунд

## 🧪 Тестирование

```bash
# Запуск всех тестов
python -m pytest tests/ -v

# Тесты с покрытием кода
python -m pytest tests/ --cov=providers --cov=analytics --cov=handlers --cov=commands --cov-report=term-missing
```

## 📈 Статистика проекта

- **84 теста** - все проходят успешно
- **84% покрытие** кода тестами
- **6 провайдеров** для разных платформ
- **2 языка** - русский и английский
- **Docker** - готов к развертыванию

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request
