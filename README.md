# ShortlyBot - Telegram Video Downloader

Telegram бот для скачивания коротких видео с популярных социальных платформ.

## 🎬 Поддерживаемые платформы

- **Instagram** - посты, reels, IGTV
- **TikTok** - короткие видео
- **YouTube Shorts** - короткие видео
- **Likee** - короткие видео
- **Facebook Reels** - короткие видео
- **RuTube** - видео и shorts

## ✨ Возможности

- 🔄 Автоматическое определение платформы по URL
- ⚡ Быстрое скачивание с оптимизированными настройками
- 🗑️ Удаление исходного сообщения после успешной отправки
- 🌍 Поддержка русского и английского языков
- 📊 Сбор статистики через RabbitMQ
- 🧪 Полное тестирование (84% покрытие)

## 🚀 Быстрый старт

### 1. Клонирование и настройка
```bash
git clone <git@github.com:voronkovd/shortlyBot.git>
cd shortlyBot
cp env.example .env
# Отредактируйте .env и добавьте ваш TELEGRAM_BOT_TOKEN
```

### 2. Запуск с Docker
```bash
# Сборка и запуск
docker-compose up -d --build

# Проверка статуса
docker-compose ps
```

### 3. Проверка работы
- **RabbitMQ Management UI**: http://localhost:15672 (admin/password123)
- **Логи бота**: `docker-compose logs -f bot`
- **Остановка**: `docker-compose down`


### Основные команды
```bash
# Статус сервисов
docker-compose ps

# Логи бота
docker-compose logs -f bot

# Перезапуск бота
docker-compose restart bot

# Очистка данных (осторожно!)
docker-compose down -v
```

## ⚠️ Ограничения

- Работает только с публичными аккаунтами
- Максимальный размер видео: 50MB
- Некоторые видео могут быть недоступны
- YouTube Shorts: только видео до 60 секунд

## 🧪 Тестирование

```bash
# Запуск всех тестов
python -m pytest tests/ -v

# Тесты с покрытием кода
python -m pytest tests/ --cov=providers --cov=analytics --cov=handlers --cov=commands --cov-report=term-missing
```