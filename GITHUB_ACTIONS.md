# 🚀 GitHub Actions для ShortlyBot

## 📋 Обзор

ShortlyBot использует GitHub Actions для автоматизации CI/CD процессов, обеспечения качества кода и безопасности.

## 🔧 Workflows

### 1. CI/CD Pipeline (`ci.yml`)

**Триггеры:**
- Push в `main` или `develop`
- Pull Request в `main`

**Задачи:**
- **Test**: Запуск тестов с RabbitMQ
- **Lint**: Проверка кода (black, isort, flake8)
- **Security**: Проверка безопасности (safety, bandit)
- **Docker**: Сборка и тестирование Docker образа
- **Deploy**: Развертывание в продакшн (при push в main)

### 2. Release (`release.yml`)

**Триггеры:**
- Push тегов `v*`

**Задачи:**
- Создание релиза на GitHub
- Генерация changelog
- Сборка и публикация Docker образа
- Загрузка артефактов (документация, deployment package)

### 3. Dependencies (`dependencies.yml`)

**Триггеры:**
- Еженедельно по понедельникам в 9:00 UTC
- Ручной запуск

**Задачи:**
- Проверка устаревших пакетов
- Сканирование безопасности
- Создание issue при обнаружении уязвимостей
- Автоматическое обновление зависимостей (при ручном запуске)

### 4. CodeQL (`codeql.yml`)

**Триггеры:**
- Push в `main`
- Pull Request в `main`
- Ежедневно в 2:00 UTC

**Задачи:**
- Анализ безопасности кода
- Поиск уязвимостей
- Генерация отчетов

## 🔐 Secrets

Для работы workflows необходимы следующие secrets:

### Обязательные:
- `TELEGRAM_BOT_TOKEN` - токен Telegram бота для тестов

### Опциональные (для Docker Hub):
- `DOCKER_USERNAME` - имя пользователя Docker Hub
- `DOCKER_PASSWORD` - пароль Docker Hub

## 📊 Мониторинг

### Статус проверок:
- ✅ **Test**: Все тесты проходят
- ✅ **Lint**: Код соответствует стандартам
- ✅ **Security**: Нет критических уязвимостей
- ✅ **Docker**: Образ собирается успешно

### Отчеты:
- **Coverage**: Отчет о покрытии кода в терминале
- **Security**: Отчеты CodeQL и Bandit
- **Dependencies**: Статус обновлений зависимостей

## 🛠️ Локальная разработка

### Предварительные проверки:

```bash
# Запуск тестов
python -m pytest tests/ -v

# Проверка стиля кода
black --check .
isort --check .
flake8 .

# Проверка безопасности
safety check
bandit -r .
```

### Docker тестирование:

```bash
# Сборка образа
docker build -t shortlybot:test .

# Тестирование
docker run --rm shortlybot:test python -c "import providers, analytics, handlers, commands, localization; print('All modules imported successfully')"
```

## 🚀 Развертывание

### Автоматическое развертывание:
1. Push в `main` → автоматический deploy
2. Создание тега `v*` → автоматический релиз

### Ручное развертывание:
```bash
# Сборка и запуск
docker-compose up -d --build

# Проверка статуса
docker-compose ps
docker-compose logs -f bot
```

## 📈 Метрики

### Покрытие кода:
- **Цель**: 80%+
- **Текущее**: 84%
- **Отчеты**: Локальные отчеты в CI

### Качество кода:
- **Linting**: black, isort, flake8
- **Security**: CodeQL, Bandit, Safety
- **Dependencies**: Dependabot

### Производительность:
- **Время сборки**: ~5-10 минут
- **Время тестов**: ~2-3 минуты
- **Время развертывания**: ~3-5 минут

## 🔧 Настройка

### Добавление новых проверок:

1. **Создать новый workflow** в `.github/workflows/`
2. **Добавить в ci.yml** как зависимость
3. **Обновить документацию**

### Настройка уведомлений:

```yaml
# В workflow файле
- name: Notify on failure
  if: failure()
  uses: 8398a7/action-slack@v3
  with:
    status: failure
    webhook_url: ${{ secrets.SLACK_WEBHOOK }}
```

## 🎯 Best Practices

1. **Быстрые проверки**: Тесты должны выполняться быстро
2. **Параллельное выполнение**: Независимые задачи выполняются параллельно
3. **Кэширование**: Используется кэш для зависимостей
4. **Безопасность**: Секреты не логируются
5. **Отчетность**: Все результаты сохраняются как артефакты

## 🐛 Troubleshooting

### Частые проблемы:

1. **Тесты падают**:
   - Проверить RabbitMQ подключение
   - Убедиться в правильности токена бота

2. **Docker сборка падает**:
   - Проверить Dockerfile
   - Убедиться в доступности базового образа

3. **Lint ошибки**:
   - Запустить `black .` и `isort .`
   - Исправить flake8 предупреждения

4. **Security предупреждения**:
   - Обновить уязвимые зависимости
   - Исправить проблемы в коде

## 📚 Полезные ссылки

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [CodeQL Documentation](https://codeql.github.com/)
- [Docker Hub](https://hub.docker.com/)

GitHub Actions обеспечивают надежность, безопасность и качество ShortlyBot! 🚀
